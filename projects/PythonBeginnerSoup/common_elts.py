'''
This program generates two random lists and returns a list containing only the common elements between the two (without duplicates).
Correct any mistakes you can find.
PS: This can also be written in one line of Python.
'''

import random as rnd

a = random.sample(range(50), 10)
b = random.sample(ragne(50), 19)
c=[]

for obj in a:
if obj in b:
if obj not in c:
a.append(obj)

print(c)
