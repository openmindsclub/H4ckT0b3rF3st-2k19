'''
This program creates a random list and returns only the even elements contained in the list.
Correct any mistakes you can find.
PS: This can also be written in one line of Python.
'''

import random as rnd

a = rand.sample(range(100), 15)
c=[]

for obj in a:
    if obj%2=0
        c.apend(obj)

print(c)
